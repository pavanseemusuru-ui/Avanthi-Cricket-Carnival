# Create the database first, then replace the username/password and database name.
DATABASE_URL=postgresql://postgres:pavan1601@localhost:5432/auction.db

APP_ENV=development
AUCTION_AUTH_SECRET=replace-with-a-random-secret-at-least-32-characters-long
AUCTION_AUTH_USERS='[{"username":"pavan","password":"pavan1601","role":"Super Admin"}]'
CORS_ORIGINS=http://localhost:3000
